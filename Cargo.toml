[package]
name = "rv"
version = "0.1.0"
edition = "2021"
description = "Generate SVG visualizations of directory structures"

[dependencies]
clap = { version = "4", features = ["derive"] }
ignore = "0.4"
